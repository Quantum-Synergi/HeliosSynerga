name: 🎯 Update Colosseum Project

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'scripts/update-colosseum.mjs'
      - '.github/workflows/update-colosseum.yml'
      - 'package.json'
      - 'package-lock.json'

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install Dependencies
        run: npm ci

      - name: 🎯 Update /my-project Metadata
        env:
          COLOSSEUM_API_KEY: ${{ secrets.COLOSSEUM_API_KEY || secrets.TRADING_API_KEY }}
          LIVE_DEMO_URL: ${{ vars.LIVE_DEMO_URL }}
        run: npm run update-colosseum
